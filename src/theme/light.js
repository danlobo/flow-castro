import { build } from "./_common";

const theme = {
  colors: {
    primary: "#0000ff",
    secondary: "#00ff00",
    background: "#ffffff44",
    text: "#000000",
    hover: "#f2f2f2",
    border: "#888",
  },
};

export default build(theme);
