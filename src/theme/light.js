import { build } from './_common'

const theme = {
  colors: {
    primary: '#0000ff',
    secondary: '#00ff00',
    background: '#ffffff',
    text: '#000000',
    hover: '#f2f2f2'
  }
}

export default build(theme)